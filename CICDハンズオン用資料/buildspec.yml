version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name work05-autodep --s3-location s3://work05-finals3/artifact.zip --source CICDハンズオン用資料/src
artifacts:
  files:
    - '**/*'
  base-directory: CICDハンズオン用資料/src